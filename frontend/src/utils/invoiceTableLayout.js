/** Invoices list: fixed actions rail (same idea as orders — no stretch/scroll from İŞLEMLER). */
/** Fits the 7×1.75rem icon grid + gap-1 + cell padding (edit/delete live in ⋮). */
export const INVOICE_ACTIONS_COL = 240;
