/** Invoices list: fixed actions rail (same idea as orders — no stretch/scroll from İŞLEMLER). */
/** Fits the 9×1.75rem icon grid + gap-1 + cell padding (sil / iptal + ⋮). */
export const INVOICE_ACTIONS_COL = 308;
